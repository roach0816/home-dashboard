apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-dashboard-data
  namespace: home-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  # storageClassName: local-path   # uncomment and set to your cluster's storage class if it has no default
